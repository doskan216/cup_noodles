<div class="mdl-grid">
  <div class="mdl-cell mdl-cell--12-col"><h2>商品一覧</h2></div>
</div>


<div class="mdl-grid">

<% @products.each do |product| %>
  <div class="mdl-cell mdl-cell--4-col">

    <!-- Square card -->
    <style>
      .demo-card-square.mdl-card {
        width: 320px;
        height: 320px;
      }
      .demo-card-square > .mdl-card__title {
        color: #fff;
      }
    </style>

    <div class="demo-card-square mdl-card mdl-shadow--2dp">
      <div class="mdl-card__title mdl-card--expand" style="background:
       url(<%= image_url("#{product.image_url}")%>) bottom right 15% no-repeat #46B6AC;">
        <h2 class="mdl-card__title-text"><%= product.name %></h2>
      </div>
      <div class="mdl-card__supporting-text">
        <%= product.description %>
      </div>
      <div class="mdl-card__actions mdl-card--border">
        <a class="mdl-button mdl-button--colored mdl-js-button mdl-js-ripple-effect">
          カートに入れる
        </a>
      </div>
    </div>
  </div>
<% end %>

</div>
